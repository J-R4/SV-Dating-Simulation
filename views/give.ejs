<!DOCTYPE html>
<html lang="en">
<body>
    <div>
        <style>
            body {
                background-image: url("https://i.pinimg.com/736x/9c/69/df/9c69df862ba31652fc278615b06bc054.jpg");
                background-repeat: no-repeat;
                background-attachment: fixed;
                background-color:rgb(154, 198, 235);
            }
            nav {
                margin-right: 10px;
                height: 40px;
                width: 600px;
            }
            #blueBox {
                position: absolute;
                vertical-align: middle;
                background-color: rgb(154, 198, 235);
                width: 600px;
                clear: both;
            }
        </style>
    </div>
    <div>
        <nav>
            <%- include('./partials/navBar.ejs') %> 
        </nav>
    </div>
    <div id ="blueBox">
        <h1>
            <%= ch.name %>
        </h1>
        <table border="1">
            <thead>
                <tr>
                    <th></th>
                    <th>Name</th>
                    <th>Gender</th>
                    <th>Hearts</th>
                    <th>Status</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td> <img src="<%= ch.pic  %>" alt="<%= ch.name %>" width="100" height="100">  </td>
                    <td><%= ch.properCall()%> </td>
                    <td><%= ch.gender %> </td>
                        <% if(rel !== null) { %>
                            <% if(ch.id === rel.CharacterId && usr === rel.UserId) {%>
                                <td> <%= rel.hearts %> </td>
                                <td> <%= rel.status %> </td>
                            <% } %> 
                        <% } %> 
                    <td> 
                        <button>
                        <a href="/character/giveIC/<%= ch.id %>" onclick="return confirm('<%= ch.likeRespond %>')">Give Ice Cream</a> 
                        </button>
                        <button>
                        <a href="/character/giveT/<%= ch.id %>"onclick="return confirm('<%= ch.dislikeRespond %>')">Give Trash</a> 
                        </button>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</body>
</html>